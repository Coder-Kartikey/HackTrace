{
  "name": "hacktrace-demo-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:sdk": "npm --prefix ../sdk run build",
    "start": "npm run build:sdk && node app.js",
    "dev": "npm run build:sdk && node app.js"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.2.1"
  }
}
